# LTS server runtime dependencies (Docker image).
# Lean by design: no audio capture (sounddevice) — that stays desktop-only.
# deepgram-sdk is required: the server's ``LTS_ENGINE=deepgram`` mode streams
# audio to Deepgram Nova-3 (imported lazily in deepgram_engine.py so the local
# Whisper mode still boots without it, but the image ships it for cloud mode).
# azure_translation_engine remains desktop-only and is not listed here.
faster-whisper>=1.2.1
numpy>=2.4.6
websockets>=17.0.1
requests>=2.34.2
python-dotenv>=1.2.3
deepgram-sdk>=6.0.0,<7.0.0
