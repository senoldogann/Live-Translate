# LTS server runtime dependencies (Docker image).
# Lean by design: no audio capture (sounddevice) or cloud SDKs (azure/deepgram)
# — those are imported lazily and only needed by the desktop app.
faster-whisper>=1.2.1
numpy>=2.4.6
websockets>=17.0.1
requests>=2.34.2
python-dotenv>=1.0.0
