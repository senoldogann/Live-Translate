# Stealth Subtitle Translator – Python Dependencies
# Requires Python 3.11+

# ── Core AI ──────────────────────────────────────────────────────────────────
# Faster-Whisper: CTranslate2-optimised Whisper implementation
faster-whisper>=1.0.0

# ArgosTranslate: Offline neural machine translation (last-resort fallback)
argostranslate>=1.9.0

# ── Audio ─────────────────────────────────────────────────────────────────────
# sounddevice: Modern audio I/O via PortAudio
sounddevice>=0.4.0

# WebRTC VAD: Voice Activity Detection
webrtcvad>=2.0.10

# ── IPC ───────────────────────────────────────────────────────────────────────
# PyZMQ: ZeroMQ bindings
pyzmq>=25.0.0

# WS API client bindings for cloud implementation 
websockets>=13.0

# ── Translation ───────────────────────────────────────────────────────────────
# deep-translator: Google Translate fallback
deep-translator>=1.9.0

# requests: HTTP client used by DeepL API client
requests>=2.28.0

# ── Numerics ──────────────────────────────────────────────────────────────────
numpy>=1.24.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
