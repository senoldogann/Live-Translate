# Stealth Subtitle Translator – Python Dependencies
# Requires Python 3.11+

# ── Core AI ──────────────────────────────────────────────────────────────────
# Faster-Whisper: CTranslate2-optimised Whisper implementation
faster-whisper>=1.2.1

# ── Audio ─────────────────────────────────────────────────────────────────────
# sounddevice: Modern audio I/O via PortAudio
sounddevice>=0.4.0

# WebRTC VAD: Voice Activity Detection
webrtcvad>=2.0.10

# ── IPC ───────────────────────────────────────────────────────────────────────
# PyZMQ: ZeroMQ bindings
pyzmq>=25.0.0

# WS API client bindings for cloud implementation 
websockets>=17.0.1
azure-cognitiveservices-speech>=1.51.2,<2.0.0
deepgram-sdk>=6.0.0,<7.0.0

# ── Translation ───────────────────────────────────────────────────────────────
# requests: HTTP client used by DeepL API client
requests>=2.34.2

# ── Numerics ──────────────────────────────────────────────────────────────────
numpy>=2.4.6

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
